
services:

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: accidents_api
    ports:
      - "8000:8000"

    volumes:
      - ./data:/app/data
      - ./src:/app/src
      - ./api:/app/api

    #environment:
    #  - PYTHONUNBUFFERED=1

