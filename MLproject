name: accidents-mlops

entry_points:
  main:
    parameters:
      n_estimators: {type: int,   default: 50}
      max_depth:    {type: int,   default: 5}
    command: "python src/models/train_model.py --n_estimators {n_estimators} --max_depth {max_depth}"
